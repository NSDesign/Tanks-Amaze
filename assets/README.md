# Custom artwork

Drop a file named `enemy-flag.svg` in this folder and the game will use it
for the enemy flag (at the base, on the ground, and carried by tanks),
scaled to flag height automatically. If the file is missing the built-in
red pennant is drawn instead.
